import Selectors from "./Selectors";

const Sidebar = () => (
  <div className="sidebar">
    <h2>Select Options</h2>
    <Selectors />
    {/* Add more selectors or options as needed */}
  </div>
);

export default Sidebar;
